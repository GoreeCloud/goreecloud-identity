# GoreeCloud Identity CI Expectations

The clean product-hardening candidate must run the applicable inherited authentik and GoreeCloud workflows on its exact head. Required failures are corrected rather than bypassed. Platform-capability skips may be documented only where the underlying GitHub capability is genuinely unavailable and the skip does not hide an application failure.
